apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-initial-only
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: resource-wasteful-app
  updatePolicy:
    updateMode: "Initial"  # Apply only to new pods
