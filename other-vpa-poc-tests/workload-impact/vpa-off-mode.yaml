apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-recommender-only
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: resource-wasteful-app
  updatePolicy:
    updateMode: "Off"  # Only provide recommendations, don't apply
